using UnityEngine;

public class ClutterClump : MonoBehaviour
{
    // - clump is several gameobjects stuck to each other.
    // - is only intended to be floating in water, so it should be buoyant.

    // - tell the clump to appear after all items in array have been marked as "knocked down".
    // - if clump is touched by player, delay before clump crumbles apart.
    // - after another delay, clump comes back


    #region Public Variables

    #endregion
}
